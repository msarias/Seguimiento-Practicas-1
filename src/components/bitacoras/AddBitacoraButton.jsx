import React from 'react';

const AddBitacoraButton = ({ toggleForm }) => {
    return (
        <button className="add-bitacora" onClick={toggleForm}>Agregar Bitácora</button>
    );
};

export default AddBitacoraButton;
